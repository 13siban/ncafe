# 메뉴 슬러그(Slug) 기반 URL 전환 계획서

본 계획서는 메뉴의 고유 식별자로서 현재 사용 중인 ID(UUID/Long)를 대신하여, 사용자가 읽기 쉽고 에이전트가 활용하기 좋은 **영문 이름 기반의 슬러그(Slug) 주소** 체계로 전환하기 위한 단계별 가이드를 담고 있습니다.

## 핵심 요구사항
1. **영문 이름 활용**: 기존 `engName` 필드를 URL 슬러그로 사용합니다.
2. **숫자 제외**: 슬러그 내에 숫자가 포함되지 않도록 제한하여 깔끔한 URL을 유지합니다.
3. **유일성(Uniqueness) 보장**: 영문 이름은 데이터베이스 수준에서 중복이 허용되지 않아야 합니다.
4. **점진적 도입**: 기존 기능을 유지하면서 단계별로 테스트하며 안전하게 전환합니다.

---

## Phase 1: 영문 이름 검증 및 데이터 무결성 확보

이 단계에서는 슬러그로 사용될 영문 이름의 형식과 유일성을 강제하는 로직을 백엔드에 구현합니다.

### 1-1. 백엔드 검증 로직 추가
- **숫자 포함 금지**: `engName` 필드에 숫자가 포함될 수 없도록 정규식(`^[a-zA-Z\s\-]+$`) 검증 로직을 Service 레이어에 추가합니다.
- **중복 체크**: 메뉴 생성(Create) 및 수정(Update) 시, 입력된 `engName`이 이미 다른 메뉴에서 사용 중인지 확인하는 로직을 추가합니다.
- **예외 처리**: 중복되거나 숫자가 포함된 경우 `400 Bad Request` 또는 커스텀 예외(`DuplicateMenuNameException`)를 반환합니다.

### 1-2. 기존 데이터 마이그레이션
- 데이터베이스 내 기존 메뉴 중 숫자가 포함된 `engName`을 수동 또는 스크립트로 정리합니다.
- 중복된 이름이 있는 경우 접미사를 붙이는 대신 관리자가 수동으로 고유한 이름을 부여하도록 처리합니다. (예: `Cafe Latte 2` -> `Signature Cafe Latte`)

### ✅ Phase 1 테스트 단계
- [ ] 관리자 페이지에서 메뉴 수정 시 숫자를 넣어보고 저장 실패 여부 확인.
- [ ] 동일한 영문 이름으로 새 메뉴 생성을 시도하여 중복 에러 발생 여부 확인.
- [ ] 기존 모든 데이터의 `engName`이 유일하고 숫자가 없는지 쿼리로 검증.

---

## Phase 2: 슬러그 기반 API 및 프론트엔드 라우팅 전환

경로 탐색 및 데이터 로딩 방식을 ID 중심에서 슬러그 중심으로 변경합니다.

### 2-1. 백엔드 조회 API 확장
- `/api/menus/{id}` 외에 `/api/menus/slug/{engName}` 형태의 조회 엔드포인트 또는 기존 조회 API에서 슬러그를 지원하도록 수정합니다.

### 2-2. 프론트엔드 라우팅 변경
- `frontend/app/menus/[id]` 디렉토리 구조를 `frontend/app/menus/[slug]` (혹은 `[engName]`)으로 변경합니다.
- 메뉴 목록 페이지에서 상세 페이지로 이동하는 모든 링크(`Link`, `router.push`)를 상품 ID 대신 슬러그를 전달하도록 수정합니다.

### 2-3. 리다이렉션 처리 (선택 사항)
- 기존 ID 기반 URL로 접속하는 사용자를 위해, ID를 받아 해당 메뉴의 슬러그로 301/302 리다이렉트 해주는 로직을 추가하여 SEO 점수를 유지합니다.

### ✅ Phase 2 테스트 단계
- [ ] 브라우저 주소창에 `ncafe.com/menus/cafe-latte`를 직접 입력했을 때 해당 메뉴 정보가 올바르게 노출되는지 확인.
- [ ] 메뉴 목록에서 상품 클릭 시 URL이 슬러그로 바뀌는지 확인.

---

## Phase 3: 에이전트 연동 및 장바구니 액션 확장

에이전트가 슬러그를 사용하여 페이지 이동 및 데이터 조작을 할 수 있도록 합니다.

### 3-1. 에이전트 시스템 지침 업데이트
- 에이전트의 System Instruction에 "상품 조작 시 반드시 슬러그(`engName`)를 사용해야 한다"는 명세와 예시를 추가합니다.
- 에이전트가 가질 수 있는 상품 지식 베이스(RAG)에도 슬러그 정보를 포함시킵니다.

### 3-2. 장바구니 담기 액션 구현 (제안)
- 에이전트가 `[ADD_TO_CART:cafe-latte, quantity:1]`와 같은 태그를 생성할 수 있도록 학습시킵니다.
- 프론트엔드 `ChatWidget`에서 해당 태그를 파싱하여, 장바구니 스토어의 액션을 호출하게 합니다.

### ✅ Phase 3 테스트 단계
- [ ] 채팅창에 "아메리카노 메뉴 페이지로 가줘"라고 했을 때 `[NAVIGATE:/menus/americano]` 태그를 생성하고 이동하는지 확인.
- [ ] "바닐라 라떼 하나 장바구니에 담아줘"라고 했을 때 올바른 슬러그로 액션이 트리거되는지 확인.
