# ☕ NCafe — 프로젝트 주요 기능 소개

> **카페 온라인 주문 & 매장 관리 풀스택 웹 애플리케이션**  
> Next.js 16 + Spring Boot + FastAPI (AI 챗봇)

---

## 📌 프로젝트 개요

| 항목 | 내용 |
|------|------|
| **프로젝트명** | NCafe (mymyy) |
| **목적** | 카페의 온라인 메뉴 조회, 주문/결제, 매장 관리를 하나의 웹 앱으로 통합 |
| **프론트엔드** | Next.js 16 (App Router), TypeScript, Zustand, CSS Modules |
| **백엔드** | Spring Boot (Java), 헥사고날 아키텍처 (Ports & Adapters) |
| **AI** | FastAPI (Python) — RAG 기반 AI 챗봇 |
| **결제** | PortOne SDK (카카오페이, 네이버페이, 이니시스) |
| **인프라** | Docker Compose |

---

## 🎯 핵심 기능 요약

```
┌────────────────────────────────────────────────────────────┐
│                    고객 기능                                │
│  메뉴 조회 · 장바구니 · 실결제 · 주문추적 · AI챗봇           │
│  마이페이지 · 등급/포인트 · 즐겨찾기 · 소셜로그인            │
├────────────────────────────────────────────────────────────┤
│                    관리자 기능                               │
│  대시보드 · 메뉴CRUD · 주문관리 · 매출분석                    │
│  회원관리 · 등급설정 · 옵션관리 · 공지팝업 · 매장설정         │
├────────────────────────────────────────────────────────────┤
│                  아키텍처 특징                               │
│  BFF패턴 · 헥사고날 · JWT보안 · 이미지압축 · SSE스트리밍      │
└────────────────────────────────────────────────────────────┘
```

---

## 1. 🛒 고객 기능

### 1.1 메뉴 조회 & 검색

- 카테고리 탭 필터링 (전체, 커피, 논커피, 디저트 등)
- 키워드 검색 (한글/영문)
- 메뉴 카드 UI (이미지, 가격, 품절/신상 뱃지 표시)
- 메뉴 상세 페이지 (이미지 갤러리, 설명, 옵션 선택)

### 1.2 메뉴 옵션 시스템

- 카테고리 기반 옵션 자동 적용 (예: 커피 카테고리 → 사이즈/샷 추가/시럽 옵션)
- 라디오(단일 선택) / 체크박스(다중 선택) 옵션 지원
- 필수 옵션 / 선택 옵션 구분
- 메뉴별 옵션 제외 설정 (특정 메뉴에서 옵션 숨기기)
- 옵션 선택에 따른 실시간 가격 반영

### 1.3 장바구니

- 메뉴 + 선택 옵션 조합별로 개별 항목 관리
- 수량 변경 시 실시간 소계/합계 재계산
- 장바구니 내에서 옵션 변경 가능 (동일 조합이면 자동 합산)
- **새로고침해도 유지** (LocalStorage 영속화)
- 매장 이용 / 포장 주문 유형 선택

### 1.4 주문 & 결제

- **실제 결제 연동** — 카카오페이, 네이버페이, 이니시스 카드결제 (PortOne SDK)
- 테스트 주문 모드 지원 (결제 없이 주문 생성)
- 회원 포인트 사용 가능 (주문 금액에서 차감)
- 비회원 주문 지원 (이름만 입력)
- 주문 확인 페이지에서 결제 전 항목 재확인

### 1.4.1 비회원 주문 vs 회원 주문 차이점

| 구분 | 비회원 | 회원 |
|------|--------|------|
| **로그인** | 불필요 | 필요 |
| **주문자 이름** | 직접 입력 (미입력 시 "비회원") | 자동으로 닉네임/유저네임 적용 |
| **결제** | ✅ 카카오/네이버/이니시스 결제 가능 | ✅ 동일 |
| **포인트 사용** | ❌ 불가 (`"포인트 결제는 회원만 가능합니다"`) | ✅ 보유 포인트로 결제 금액 차감 |
| **포인트 적립** | ❌ 적립 안됨 | ✅ 등급별 적립률에 따라 자동 적립 |
| **등급 반영** | ❌ 등급 시스템 미적용 | ✅ 주문 금액이 등급 산정에 반영 |
| **주문 조회** | 주문번호/날짜를 기억해야 조회 가능 | `/order/my`에서 전체 주문 내역 조회 가능 |
| **주문 추적** | ✅ 주문번호만 알면 누구나 조회 가능 | ✅ 본인 주문만 조회 가능 (403 차단) |
| **주문 내역 저장** | 브라우저 LocalStorage에 임시 저장 (최대 20건) | 서버 DB에 영구 저장 (userId 연동) |
| **마이페이지** | ❌ 이용 불가 | ✅ 주문 내역, TOP 메뉴, 즐겨찾기, 포인트 |
| **즐겨찾기** | ❌ 불가 | ✅ 메뉴+옵션 조합 즐겨찾기 저장 |

#### 주문 프로세스 비교

```
회원 주문 흐름:
  장바구니 → 주문확인 → 포인트 사용 선택 → 결제
                         │
                   결제 금액 = 원가 - 포인트
                         │
                   주문 완료 → 등급별 적립률로 포인트 적립
                            → 주문 금액이 등급 산정에 반영
                            → 서버 DB에 주문 내역 저장 (userId 연동)

비회원 주문 흐름:
  장바구니 → 주문확인 → 이름 입력 → 결제
                                    │
                              결제 금액 = 원가 (포인트 없음)
                                    │
                              주문 완료 → 포인트 적립 없음
                                       → LocalStorage에 주문번호 임시 저장
```

#### 주문 조회 보안 정책

```
GET /orders/{date}/{number}

비회원 주문 (userId = null)
  → 주문번호만 알면 누구나 조회 가능 (오픈 접근)

회원 주문 (userId 존재)
  → 로그인한 본인만 조회 가능
  → 다른 사용자 접근 시 403 Forbidden 반환
```

### 1.5 실시간 주문 추적

- 주문 완료 후 `주문접수 → 제조중 → 제조완료 → 픽업완료` 상태 추적
- 실시간 폴링으로 상태 자동 업데이트
- **전역 주문 추적 바** — 어떤 페이지에서든 주문 상태 확인 가능
- 주문 번호(`#1`, `#2`, ...)로 간편 조회

### 1.6 AI 챗봇 (RAG 기반)

- 화면 우측 하단 플로팅 위젯으로 상시 접근
- **SSE(Server-Sent Events) 스트리밍** — 응답이 한 글자씩 실시간 렌더링
- 자연어로 메뉴 추천, 주문, 페이지 이동 가능
- **Function Calling** 지원:
  - `"아메리카노 하나 담아줘"` → 장바구니에 자동 추가
  - `"주문 내역 보여줘"` → 주문 내역 페이지로 이동
  - `"따뜻한 음료 추천해줘"` → 메뉴 카드 리스트 표시
  - `"지난번에 주문했던 거 다시 시켜줘"` → 이전 주문 재주문
  - `"이 메뉴 즐겨찾기에 추가"` → 즐겨찾기 패널 열기
- 대화 히스토리 유지 (세션별 관리)

### 1.7 회원 시스템

- **일반 회원가입 / 로그인**
- **구글 소셜 로그인** (@react-oauth/google)
- JWT 기반 인증이지만, **BFF 패턴**으로 토큰은 서버 사이드에서만 관리 (보안)
- 계정 탈퇴(soft delete) & 복구 기능

### 1.8 마이페이지

| 탭 | 기능 |
|----|------|
| **등급 및 포인트** | 현재 등급, 다음 등급까지 진행 상황, 포인트 잔액, 적립/사용 내역 |
| **주문 및 관심 메뉴** | 최근 주문 내역, 자주 주문한 메뉴 TOP 5, 즐겨찾기 목록 |
| **회원정보 수정** | 닉네임/이메일/전화번호 수정, 비밀번호 변경, 계정 탈퇴 |

### 1.9 등급 & 포인트

- 주문 금액에 따른 **자동 등급 산정** (예: Bronze → Silver → Gold → Diamond)
- 등급별 **포인트 적립률** 차등 적용
- 주문 시 포인트 사용 가능 (결제 금액 차감)
- 포인트 적립/사용 내역 조회

### 1.10 즐겨찾기

- 메뉴 + 옵션 조합을 즐겨찾기로 저장
- 즐겨찾기에서 바로 장바구니 추가 가능
- 마이페이지에서 즐겨찾기 목록 관리

---

## 2. 🔧 관리자 기능

### 2.1 대시보드

- **오늘의 통계** — 총 매출, 주문 수, 완료율, 방문 고객 수
- **일간 / 주간 / 월간** 탭 전환
- 최근 주문 5건 실시간 표시
- 인기 메뉴 TOP 5
- 제조 대기중 주문 알림 배너
- 영업 시작/종료 토글 버튼
- 빠른 실행 버튼 (메뉴 등록, 주문 관리 등)

### 2.2 메뉴 관리 (CRUD)

- 메뉴 등록 / 수정 / 삭제
- **그리드 뷰 / 리스트 뷰** 전환
- 카테고리별 필터 + 키워드 검색
- 이미지 업로드 (드래그 앤 드롭, 자동 WebP 압축)
- 메인 이미지 설정 + 멀티 이미지 갤러리
- 품절 처리 / 판매 중지 토글
- 정렬 순서 변경 (드래그 앤 드롭)
- **일괄 수정** — 복수 메뉴의 가격/상태를 한 번에 변경

### 2.3 카테고리 관리

- 카테고리 생성 / 수정 / 삭제
- 정렬 순서 관리

### 2.4 옵션 관리

- 옵션 그룹 생성 (예: 사이즈, 샷 추가, 시럽)
- 옵션 아이템 관리 (이름, 추가 가격)
- 라디오(단일 선택) / 체크박스(다중 선택) 타입 설정
- **카테고리 ↔ 옵션 매핑** — 특정 카테고리에 옵션 그룹 자동 적용
- **메뉴별 옵션 제외** — 특정 메뉴에서 옵션 숨기기

### 2.5 주문 관리

- 주문 목록 (날짜/상태별 필터)
- 주문 상태 변경 (`접수됨 → 제조중 → 제조완료 → 픽업완료`)
- 주문 거절 (거절 사유 입력)
- 주문 상세 모달 (주문 아이템, 옵션, 결제 정보)

### 2.6 매출 분석

- **일별 / 주별 / 월별** 기간 전환
- 매출 요약 통계 (총 매출, 주문 수, 객단가, 결제 건수)
- **라인 차트** — 기간별 매출 추이 시각화 (Recharts)
- **파이 차트** — 카테고리별 판매 비율
- **메뉴 랭킹** — 상품별 판매량/매출 순위 테이블
- 기간별 주문 내역 테이블 (주문 상세 모달 연동)

### 2.7 회원 관리

- 전체 회원 목록 조회 (이름, 이메일, 역할, 등급)
- 회원 역할 변경 (일반 → 부관리자 → 관리자)
- 등급 수동 변경
- 포인트 수동 지급
- 계정 잠금/해제

### 2.8 등급 설정

- 등급별 기준 설정 (누적 주문 금액, 주문 횟수)
- 등급별 포인트 적립률 설정
- 등급 자동 산정 on/off 설정
- 등급 순서 변경

### 2.9 매장 설정

- 카페 이름, 설명, 연락처, 주소 수정
- 영업 시간 설정 (오픈/마감 시간)
- 파비콘 업로드 (라이트/다크 모드 대응)
- 영업 시작/종료 수동 제어

### 2.10 공지 팝업 관리

- 팝업 생성 / 수정 / 삭제
- 이미지 업로드
- **게시 기간 설정** (시작일/종료일)
- 해당 기간에만 고객 화면에 자동 표시

### 2.11 AI RAG 관리

- 챗봇 학습 데이터 관리

---

## 3. 🏗️ 아키텍처 & 기술적 포인트

### 3.1 BFF (Backend For Frontend) 패턴

```
브라우저  ─── fetch('/api/메뉴') ───▶  Next.js 서버 (BFF)
                                        │
                                  JWT 자동 주입
                                  이미지 자동 압축
                                        │
                                        ▼
                                  Spring Boot 백엔드
```

- 클라이언트가 백엔드와 **직접 통신하지 않음**
- JWT를 `httpOnly 쿠키`로 관리하여 **XSS 공격 방어**
- 이미지 업로드 시 **자동 WebP 변환 + 리사이즈** (Sharp 라이브러리)
- Spring Boot(8080)과 FastAPI(8000)를 **하나의 API 엔드포인트**로 통합

### 3.2 헥사고날 아키텍처 (백엔드)

```
Controller (Adapter In)
    │
    ▼
UseCase Interface (Port In)
    │
Service (구현체)
    │
    ▼
RepositoryPort (Port Out)
    │
    ▼
PersistenceAdapter (Adapter Out) → DB
```

- **Domain 모델이 JPA에 의존하지 않음** (순수 POJO)
- Port(인터페이스)를 통한 느슨한 결합
- 같은 엔티티라도 **고객용(조회)과 관리자용(CRUD)을 별도 모듈로 분리**

### 3.3 인증 보안

| 기술 | 설명 |
|------|------|
| iron-session | 서버 사이드 세션 관리, httpOnly 쿠키 |
| JWT | Spring Boot에서 발급, Next.js BFF에서만 접근 |
| Next.js Middleware | 라우트별 인증/권한 체크 (관리자 페이지 보호) |
| 구글 OAuth | 소셜 로그인 지원 |

### 3.4 실시간 기능

- **AI 챗봇 SSE** — 응답을 한 글자씩 스트리밍 전달
- **주문 상태 폴링** — 주문 상태 변경을 주기적으로 확인
- **전역 주문 추적** — 모든 페이지에서 주문 상태 표시

### 3.5 반응형 디자인

- 모든 고객/관리자 페이지 **모바일 대응**
- 관리자 사이드바: 데스크탑(펼침/접힘), 모바일(오버레이)
- CSS Modules + CSS 변수로 일관된 디자인 시스템

---

## 4. 📄 페이지 맵

### 고객 페이지

```
/                     ← 홈 (히어로 배너, 시그니처 메뉴, 등급 혜택)
/menus                ← 메뉴 목록 (카테고리 탭, 검색)
/menus/[slug]         ← 메뉴 상세 (이미지 갤러리, 옵션, 장바구니 추가)
/cart                 ← 장바구니 (수량/옵션 변경, 합계)
/order/confirm        ← 주문 확인 & 결제
/order/[날짜]/[번호]   ← 주문 추적 (실시간 상태)
/order/my             ← 내 주문 내역
/mypage               ← 마이페이지 (등급/포인트/주문/프로필)
/login                ← 로그인 (일반 + 구글)
/signup               ← 회원가입
/about                ← 매장 소개 (위치, 갤러리, 파트너)
```

### 관리자 페이지

```
/admin                ← 대시보드 (매출, 최근 주문, 인기 메뉴)
/admin/menus          ← 메뉴 관리 (그리드/리스트, CRUD)
/admin/menus/new      ← 메뉴 등록
/admin/menus/[id]     ← 메뉴 수정
/admin/categories     ← 카테고리 관리
/admin/options        ← 옵션 관리
/admin/orders         ← 주문 관리 (상태 변경)
/admin/users          ← 회원 관리 (역할/등급/잠금)
/admin/sales          ← 매출 분석 (차트/랭킹)
/admin/settings       ← 매장 설정
/admin/notice-popups  ← 공지 팝업
/admin/grade-settings ← 등급 설정
/admin/rag            ← AI RAG 관리
```

---

## 5. 🔗 기술 스택 전체

### 프론트엔드

| 기술 | 용도 |
|------|------|
| Next.js 16 (App Router) | SSR, 파일 기반 라우팅, API Routes (BFF) |
| React 19 | UI 렌더링 |
| TypeScript | 타입 안전성 |
| Zustand | 전역 상태 관리 (인증, 장바구니, 챗봇) |
| CSS Modules + CSS Variables | 스코프 스타일링 + 디자인 토큰 |
| Framer Motion | 애니메이션 |
| Recharts | 차트 (매출 분석) |
| React Hook Form | 폼 관리 |
| @dnd-kit | 드래그 앤 드롭 (정렬) |
| PortOne SDK | 실결제 연동 |
| iron-session | 서버 세션 |
| Sharp | 이미지 압축/변환 |
| Lucide React | 아이콘 |
| react-hot-toast | 토스트 알림 |

### 백엔드

| 기술 | 용도 |
|------|------|
| Spring Boot | REST API 서버 |
| Spring Security + JWT | 인증/인가 |
| Spring Data JPA | DB 접근 |
| PostgreSQL | 데이터베이스 |
| FastAPI (Python) | AI 챗봇 서버 |
| Docker Compose | 컨테이너 오케스트레이션 |

---

## 6. 🎨 디자인 특징

- **그린 기반 컬러 팔레트** (`#3d6d64`) — 카페의 자연스럽고 고급스러운 느낌
- **Sweet(SUITE) + Pretendard** 폰트 — 가독성 높은 한글 디자인
- **Fraunces** 세리프 폰트 — 영문 포인트 텍스트에 프리미엄 감성
- 스크롤 애니메이션, 호버 효과, 부드러운 전환 등 **마이크로 인터랙션**
- 다크 모드 대응 파비콘
